---
description: Polish an existing LitSquare Stage animation and export checked artifacts.
argument-hint: PROJECT_PATH= NOTES="..."
---

Use the LitSquare Stage Animation plugin to polish the animation and export checked artifacts:

$ARGUMENTS

Inspect the existing project, improve motion, timing, layout, typography, and asset handling, run checks, then render through the LitSquare Stage macOS app when available.
