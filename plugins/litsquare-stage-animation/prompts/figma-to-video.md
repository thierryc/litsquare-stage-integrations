---
description: Convert Figma frame input into a LitSquare Stage animation and video render.
argument-hint: FIGMA_SOURCE= OUTPUT_FORMAT=
---

Use the LitSquare Stage Animation plugin to convert this Figma source into local LitSquare Stage HTML, CSS, and JavaScript:

$ARGUMENTS

Extract layout, typography, colors, assets, and SVG paths; create deterministic motion; build; compare stills to the source; then render through the mandatory LitSquare Stage macOS app.
