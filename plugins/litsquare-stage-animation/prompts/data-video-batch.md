---
description: Generate multiple LitSquare Stage videos from API, JSON, or CSV data.
argument-hint: DATA_SOURCE= TEMPLATE= VARIANTS=
---

Use the LitSquare Stage Animation plugin to create a data-driven video batch:

$ARGUMENTS

Snapshot input data locally, validate the schema, generate stable variant configs, render each variant through the LitSquare Stage macOS app, and write an artifact manifest.
