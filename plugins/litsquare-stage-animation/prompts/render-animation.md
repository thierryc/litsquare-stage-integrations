---
description: Render a LitSquare Stage project through the mandatory macOS app.
argument-hint: PROJECT_PATH= OUTPUT= FORMAT=
---

Use the LitSquare Stage Animation plugin to render this project:

$ARGUMENTS

First verify macOS and LitSquare Stage app availability. If the app is missing, stop with the mandatory-app message. Otherwise build the project, render the requested output, inspect the artifact, and report paths and settings.
