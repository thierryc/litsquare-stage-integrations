interface:
  display_name: "LitSquare Stage Render Video"
  short_description: "Natively render and encode video in LitSquare Stage"
  default_prompt: "Use $litsquare-stage-render-video to render and natively encode this video in the LitSquare Stage app."
policy:
  allow_implicit_invocation: true
