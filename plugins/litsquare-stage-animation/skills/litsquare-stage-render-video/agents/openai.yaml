interface:
  display_name: "LitSquare Stage Render Video"
  short_description: "Render LitSquare Stage stills, sequences, and videos through the mandatory macOS app"
  default_prompt: "Render this LitSquare Stage project to video through the LitSquare Stage app."
policy:
  allow_implicit_invocation: true
